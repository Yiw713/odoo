<!-- <odoo>
    <record id="crm_custom_kanban_inherit" model="ir.ui.view">
        <field name="name">crm.lead.kanban.custom</field>
        <field name="model">crm.lead</field>
        <field name="inherit_id" ref="crm.crm_case_kanban_view_leads"/>
        <field name="priority" eval="20"/>
        <field name="arch" type="xml">

            <!-- 1️⃣ แปล Lost Ribbon -->
            <xpath expr="//widget[@id='lost_ribbon']" position="attributes">
                <attribute name="title">แพ้การขาย</attribute>
            </xpath>

            <!-- 2️⃣ เพิ่ม Probability Badge ใต้ชื่อ -->
            <xpath expr="//field[@name='name']" position="after">
                <div class="mt-1">
                    <span class="badge bg-info">
                        <field name="probability"/> %
                    </span>
                </div>
            </xpath>

            <!-- 3️⃣ ทำให้ Probability สูงเป็นสีเขียว -->
            <xpath expr="//t[@t-name='card']" position="inside">
                <t t-if="record.probability.raw_value &gt;= 70">
                    <div class="badge bg-success mb-1">
                        High Chance
                    </div>
                </t>
            </xpath>

            <!-- 4️⃣ เพิ่มปุ่ม Quick View -->
            <xpath expr="//footer" position="before">
                <div class="mt-2">
                    <a type="open"
   role="button"
   class="btn btn-sm btn-outline-primary w-100">
    ดูรายละเอียด
</a>
                </div>
            </xpath>

        </field>
    </record>
</odoo> -->