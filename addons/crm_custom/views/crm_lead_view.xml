<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="crm_lead_form_inherit_custom" model="ir.ui.view">
        <field name="name">crm.lead.form.inherit.custom</field>
        <field name="model">crm.lead</field>
        <field name="inherit_id" ref="crm.crm_lead_view_form"/>
        <field name="arch" type="xml">

            <!-- เพิ่ม Tab ใหม่ -->
            <xpath expr="//notebook" position="inside">
                <page string="Info">
                    <group>
                        <field name="x_customer_budget"/>
                        <field name="x_project_deadline"/>
                    </group>
                </page>
            </xpath>

        </field>
    </record>

    <record id="crm_lead_form_inherit_custom_btn" model="ir.ui.view">
        <field name="name">crm.lead.form.inherit.smart.button</field>
        <field name="model">crm.lead</field>
        <field name="inherit_id" ref="crm.crm_lead_view_form"/>
        <field name="arch" type="xml">

            <xpath expr="//div[@class='oe_button_box']" position="inside">
                <button type="object"
                        name="action_view_logs"
                        class="oe_stat_button"
                        icon="fa-list">
                    <field name="x_log_count" widget="statinfo"/>
                </button>
            </xpath>

        </field>
    </record>
</odoo>

